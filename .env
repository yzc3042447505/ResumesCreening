
# 大模型API地址（非敏感信息，可保留默认值）
LLM_BASE_URL=https://ark.cn-beijing.volces.com/api/v3

# 模型名称（非敏感信息，可保留默认值）
LLM_MODEL=ep-20260813161459-mjmvd

# 应用配置
MAX_RESUME_COUNT=20

# 运行模式：local=本地持久化模式（数据存数据库），不设置=线上临时模式（数据存会话，重启清除）
APP_ENV=local
